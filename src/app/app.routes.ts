/**
 * Configuration du routage de l'application
 * 
 * Ce fichier définit toutes les routes disponibles dans l'application.
 * Actuellement vide car l'application utilise une approche single-page
 * avec tous les composants visibles simultanément.
 * 
 * Les routes seraient à ajouter ici si une navigation entre pages était nécessaire.
 * 
 * @example
 * export const routes: Routes = [
 *   { path: '', component: MapComponent },
 *   { path: 'projects', component: ProjectListComponent },
 *   { path: '**', redirectTo: '' }
 * ];
 */

import { Routes } from '@angular/router';

// Configuration vide car l'application est single-page
export const routes: Routes = [];
